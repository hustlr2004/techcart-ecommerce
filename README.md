# TechCart — Full Stack E-Commerce Platform

## Setup
1. Clone the repo
2. `cd server && cp .env.example .env && fill values`
3. `cd client && cp .env.example .env && fill values`
4. `cd server && npm install && npm start`
5. `cd client && npm install && npm start`
